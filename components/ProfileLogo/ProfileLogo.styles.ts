// Standard packages
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    aspectRatio: 1,
    borderRadius: 1000,
    justifyContent: "center",
    marginBottom: 8,
    width: "25%",
  },
  icon: { fontSize: 36, fontWeight: "bold" },
});
